# CPU 冒烟·Gumbel 模式（DESIGN §8）：验证 Gumbel 训练搜索全流水线（不追求棋力）
# 与 smoke.yaml 仅差 mcts.algorithm/gumbel_m；num_sims=24 下 m=8 → SH 3 phases:
# 8×1 + 4×2 + 2×4 = 24，预算恰好用满。
model: {blocks: 2, filters: 32, history_steps: 2}
mcts: {num_sims: 24, batch_size: 8, temp_moves: 8, algorithm: gumbel, gumbel_m: 8}
selfplay:
  games_per_iteration: 4
  num_workers: 2
  games_per_worker: 2
  eval_max_batch: 64
  eval_wait_ms: 1.0
  max_game_plies: 60
  resign_enabled: false
  value_blend_iters: 2
train:
  device: cpu
  num_gpus: 0
  batch_size: 64
  train_steps_per_iteration: 20
  buffer_window: 50000
  min_buffer_to_train: 64
  total_iterations: 2
  # 隔离输出目录——rl/train.py 启动时会无条件把随机初始网写成 <ckpt_dir>/best.pt,
  # 不设此项直接跑本配置会【覆盖主线 ckpts/best.pt】(就绪评审 M2 地雷)。
  checkpoint_dir: ckpts_smoke_gumbel
  amp: false
arena: {arena_games: 2, arena_sims: 16}
log: {log_dir: logs_smoke_gumbel, records_dir: records_smoke_gumbel,
      log_interval_sec: 5.0, train_log_interval: 5, tensorboard: false}
